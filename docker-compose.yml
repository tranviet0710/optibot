version: "3.8"

services:
  optisigns-bot:
    build:
      context: .
      dockerfile: Dockerfile
    image: optisigns-bot
    container_name: optisigns-bot-container
    env_file:
      - .env
    # Uncomment the next lines if you want to mount your code for live development
    # volumes:
    #   - .:/app
    command: python main.py
